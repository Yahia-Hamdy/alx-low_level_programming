#include "main.h"
/**
 * print_more_numbers - prints the numbers, from 0 to 14, followed by a new line
 *
 */
void print_more_numbers(void)
{
int i, count;
for (count =1; count < 10; count ++)
	{
	for (i = 0; i < 15; i++)
	{
		_putchar('0' + i);
	}
	_putchar('\n');
	}
}
